[build]
	command = "npm run build"
	publish = "dist"

[[headers]]
	for = "/*"

	[headers.values]
		cache-control = '''
		public,
		max-age=31536000,
		must-revalidate'''

	for = "/assets/**/*"

	[headers.values]
		cache-control = '''
		public,
		max-age=31536000,
		must-revalidate'''
